{
  "name": "url_shortner",
  "version": "1.0.0",
  "type": "module",
  "description": "1. Design a HTML form where user will enter long url.\r 2. Design an API end point which takes long url as input and returns short url as output.\r 3. Design an end point which takes short url and redirects the user to actual (original / long) url.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "nanoid": "^5.0.7",
    "nodemon": "^3.1.4"
  }
}
